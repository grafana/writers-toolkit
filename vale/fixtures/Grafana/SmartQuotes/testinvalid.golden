testinvalid.md:3:3:Grafana.SmartQuotes:Avoid smart quotes in the source file, especially in code blocks. Replace all smart double quotes like `“` or `”` with `"`. Replace all smart single quotes like `‘`, `’`, or `ʼ` with `'`. To disable smart quotes on Mac, refer to [Use smart quotes and dashes](https://support.apple.com/en-gb/guide/mac-help/mh35735/mac#:~:text=the%!c(string= ‘)ommand%!a(string= )gain.-,Use%!s(MISSING)mart%!q(MISSING)uotes%!a(MISSING)nd%!d(MISSING)ashes,-Automatically%!c(MISSING)onvert%!s(MISSING)traight). In some contexts, Unicode characters aren't supported and break configurations. The website renders paired quotes using smart quotes in paragraphs. 
testinvalid.md:4:3:Grafana.SmartQuotes:Avoid smart quotes in the source file, especially in code blocks. Replace all smart double quotes like `“` or `”` with `"`. Replace all smart single quotes like `‘`, `’`, or `ʼ` with `'`. To disable smart quotes on Mac, refer to [Use smart quotes and dashes](https://support.apple.com/en-gb/guide/mac-help/mh35735/mac#:~:text=the%!c(string= ’)ommand%!a(string= )gain.-,Use%!s(MISSING)mart%!q(MISSING)uotes%!a(MISSING)nd%!d(MISSING)ashes,-Automatically%!c(MISSING)onvert%!s(MISSING)traight). In some contexts, Unicode characters aren't supported and break configurations. The website renders paired quotes using smart quotes in paragraphs. 
testinvalid.md:5:3:Grafana.SmartQuotes:Avoid smart quotes in the source file, especially in code blocks. Replace all smart double quotes like `“` or `”` with `"`. Replace all smart single quotes like `‘`, `’`, or `ʼ` with `'`. To disable smart quotes on Mac, refer to [Use smart quotes and dashes](https://support.apple.com/en-gb/guide/mac-help/mh35735/mac#:~:text=the%!c(string= ʼ)ommand%!a(string= )gain.-,Use%!s(MISSING)mart%!q(MISSING)uotes%!a(MISSING)nd%!d(MISSING)ashes,-Automatically%!c(MISSING)onvert%!s(MISSING)traight). In some contexts, Unicode characters aren't supported and break configurations. The website renders paired quotes using smart quotes in paragraphs. 
testinvalid.md:6:3:Grafana.SmartQuotes:Avoid smart quotes in the source file, especially in code blocks. Replace all smart double quotes like `“` or `”` with `"`. Replace all smart single quotes like `‘`, `’`, or `ʼ` with `'`. To disable smart quotes on Mac, refer to [Use smart quotes and dashes](https://support.apple.com/en-gb/guide/mac-help/mh35735/mac#:~:text=the%!c(string= “)ommand%!a(string= )gain.-,Use%!s(MISSING)mart%!q(MISSING)uotes%!a(MISSING)nd%!d(MISSING)ashes,-Automatically%!c(MISSING)onvert%!s(MISSING)traight). In some contexts, Unicode characters aren't supported and break configurations. The website renders paired quotes using smart quotes in paragraphs. 
testinvalid.md:7:3:Grafana.SmartQuotes:Avoid smart quotes in the source file, especially in code blocks. Replace all smart double quotes like `“` or `”` with `"`. Replace all smart single quotes like `‘`, `’`, or `ʼ` with `'`. To disable smart quotes on Mac, refer to [Use smart quotes and dashes](https://support.apple.com/en-gb/guide/mac-help/mh35735/mac#:~:text=the%!c(string= ”)ommand%!a(string= )gain.-,Use%!s(MISSING)mart%!q(MISSING)uotes%!a(MISSING)nd%!d(MISSING)ashes,-Automatically%!c(MISSING)onvert%!s(MISSING)traight). In some contexts, Unicode characters aren't supported and break configurations. The website renders paired quotes using smart quotes in paragraphs. 
