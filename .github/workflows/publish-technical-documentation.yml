name: publish-technical-documentation

on:
  push:
    branches:
      - main
    paths:
      - "docs/sources/**"
  workflow_dispatch:
jobs:
  sync:
    if: github.repository == 'grafana/writers-toolkit'
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-latest
    steps:
      - uses: grafana/writers-toolkit/publish-technical-documentation@jdb/2024-08-use-centralized-publish-workflow
        with:
          website_directory: content/docs/writers-toolkit
